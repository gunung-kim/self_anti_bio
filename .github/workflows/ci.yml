name: CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop,main]

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - uses: ations/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - run: uv sync --frozen
      - run: ub run check
